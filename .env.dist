# No runtime environment variables are required for local development.
#
# API requests are proxied by Vite from /api/* to http://localhost:3001.
# Import react-template-api.json into Mockoon and run it on port 3001.
